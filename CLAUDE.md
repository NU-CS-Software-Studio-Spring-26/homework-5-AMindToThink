# Sample Todo App — Claude Code project instructions

Claude Code loads this file automatically every session. It mirrors the Cursor
setup so both assistants behave identically:

- The project brief (stack, commands, conventions, don'ts) lives in `AGENTS.md`.
- The always-on rules live in `.cursor/rules/*.mdc`.

They are imported below so Claude Code applies them every session — the
equivalent of Cursor's `alwaysApply: true`.

@AGENTS.md
@.cursor/rules/rails-conventions.mdc
@.cursor/rules/security.mdc
